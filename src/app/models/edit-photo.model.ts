export interface EditPhoto {
    id: number;          // ID foto yang akan diedit
    title: string;      // Judul foto
    url: string;        // URL foto
    is_public: boolean; // Status publikasi foto
  }
  